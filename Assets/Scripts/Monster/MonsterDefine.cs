/// <summary>
/// 인스턴스를 생성하지 않는 Static 클래스로 제작한다.
/// </summary>
public static class MonsterDefine
{
    public enum MONSTER_BEHAV
    {
        Null = 0,
        GroundWalk,
        GroundJump,
        GroundFloting,
        Fly,
    }
}
