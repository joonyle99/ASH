using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class InteractionType
{
    public enum Type
    {
        NULL = 0,

        PUSH,
        PULL,
        TALK,

    }
}
