using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Utils
{
    public static class Gizmos
    {
        // TODO
        /*
        public float DrawArrow(Color color, Vector3 from, Vector3 to, float headSize = 0.5f)
        {
            UnityEngine.Gizmos.color = color;
            UnityEngine.Gizmos.DrawLine(from, to);

            Vector3[] headPoints = new Vector3[3];
            headPoints[0] = to + (from - to).normalized * headSize;
            headPoints[1] = to + new Vector3();
            headPoints[2] = to + new Vector3();
            UnityEngine.Gizmos.DrawLine(from, to);
            UnityEngine.Gizmos.DrawLine(from, to);
            UnityEngine.Gizmos.DrawLine(from, to);

        }
        */
    }

}